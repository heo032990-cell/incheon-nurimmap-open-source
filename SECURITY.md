# 보안 및 개인정보

실제 접속 키·비밀번호·JWT·service_role 키·Google 배포 주소·Drive 폴더 식별자·신청자 자료를 저장소나 공개 이슈에 올리지 마세요. 설정 예시는 빈 값으로 제공합니다.

브라우저 설정에는 본인 프로젝트의 공개용 publishable 키만 사용합니다. service_role 키와 Google 연계 secret은 서버 설정에만 저장합니다. 공개용 키는 브라우저에서 보일 수 있으므로 데이터 보호는 RLS와 서버 권한 검사로 구현해야 합니다.

config.local.js 및 .env는 Git에서 제외됩니다. 배포 산출물에는 브라우저용 설정이 포함되므로 서버 비밀 값을 입력하지 마세요. 검사는 패턴 기반이며 모든 비밀정보 유출이나 취약점을 보장해서 탐지하지 않습니다.

의심되는 취약점은 개인정보 없는 재현 요약으로 담당 기관에 먼저 알리세요. 실제 이용자 자료를 이용한 시험 대신 별도 시험 프로젝트와 가상 정보를 사용하세요.
