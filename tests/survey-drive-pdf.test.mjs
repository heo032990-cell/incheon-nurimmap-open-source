import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';
const code=fs.readFileSync(new URL('../backend/google-apps-script.gs',import.meta.url),'utf8');let html='',created=0,name='';const blob={setName:n=>{name=n;return blob},getBytes:()=>[1,2]};const ctx={PropertiesService:{getScriptProperties:()=>({getProperty:()=>""})},HtmlService:{createHtmlOutput:h=>{html=h;return {getAs:m=>{assert.equal(m,'application/pdf');return blob}}}},MimeType:{PDF:'application/pdf'}};vm.runInNewContext(code,ctx);assert(!code.includes('DocumentApp'));
let exists=false;const file={getUrl:()=> 'test.pdf'},folder={getFilesByName:()=>({hasNext:()=>exists,next:()=>file}),createFile:b=>{assert.equal(b,blob);created++;exists=true;return file}};
const schema={title:'신청서',description:'안내',questions:[{id:'x',type:'text',label:'질문',help:'',options:[],required:true}]};
assert.equal(ctx.nurimPdf_(folder,'test.pdf',schema,{name:'<script>bad</script>',birth:'2000-01-02',phone:'01000000000',answers:{x:'답변'}}),file);assert.equal(name,'test.pdf');assert(html.includes('&lt;script&gt;'));assert(html.includes('2000년 01월 02일'));assert(!html.includes('<button'));assert(html.includes('답변'));ctx.nurimPdf_(folder,'test.pdf',schema,null);assert.equal(created,1);assert(ctx.authorizeNurimSurveyV78().includes('확인 완료'));console.log('No DocumentApp dependency; HTML escaping, record content, PDF conversion path and retry reuse passed (Google services mocked)');
