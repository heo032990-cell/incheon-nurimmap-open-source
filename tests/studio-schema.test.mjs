import assert from 'node:assert/strict';import {validateSchema,validateAnswers,surveyPages} from '../survey-core.mjs';
const header=(id,label,br)=>({id,type:'notice',label,help:'페이지 안내',required:false,options:[],pageBreakBefore:br});const schema=validateSchema({title:'페이지 설문',description:'',questions:[header('page_first','첫 페이지',false),{id:'a',type:'text',label:'의견',help:'',options:[]},header('page_next','둘째 페이지',true),{id:'confirm',type:'checkbox',label:'준비물 확인',help:'실내화를 준비하세요',options:['동의합니다'],selectionMode:'all',required:true},{id:'choice',type:'consent',label:'선택 동의',help:'거부 가능',required:true,blockRefusal:false,options:[]}]});
const stored=JSON.parse(JSON.stringify(schema));assert.equal(surveyPages(stored).length,2);assert.equal(surveyPages(stored)[1][0].help,'페이지 안내');const basic={name:'시험',birth:'2000-01-01',phone:'01000000000'};assert.throws(()=>validateAnswers(stored,basic,{confirm:[],choice:'disagree'}));assert.deepEqual(validateAnswers(stored,basic,{confirm:['동의합니다'],choice:'disagree'}).confirm,['동의합니다']);assert.equal(validateAnswers(stored,basic,{confirm:['동의합니다'],choice:'disagree'}).choice,'disagree');console.log('PASS: persisted page descriptions, required single consent, optional refusal and legacy payload compatibility');